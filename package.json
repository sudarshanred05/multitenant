{
  "name": "shopify-insights-service",
  "version": "1.0.0",
  "description": "Shopify Insights Service - Full Stack Application",
  "main": "backend/src/server.js",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/shopify-insights-service.git"
  },
  "keywords": [
    "shopify",
    "analytics",
    "insights",
    "dashboard",
    "react",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}